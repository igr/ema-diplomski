\begin{figure}[!ht]
\centering
\vspace{180pt}%
\begin{tikzpicture}[transform canvas={scale=0.6}]
\begin{scope}[yshift=140pt]
  

  \tikzstyle{every node}=[font=\fontsize{20}{20}\selectfont]

  %%% vars
  \def\r{4}
  \def\rd{4.5}
  \def\rr{5}
  \def\w{6}
  \def\alpha{30}
  \def\beta{60}

  %%% x & y axis
  \draw[->]
    (-\rr,0) -- (\rr,0) node[right] {$x$};
  \draw[->]
    (0,-\rr) -- (0,\rr) node[above] {$y$};

  \coordinate (O) at (0,0);

  \coordinate (left) at ({-\rd},{-\rd*tan(\alpha)});
  \coordinate (right) at ({\rd},{\rd*tan(\alpha)});
  \coordinate (rightAux) at ({\r},{\r*tan(\alpha)});
  \coordinate (rightCircle) at ({\r*cos(\alpha)},{\r*sin(\alpha)});
  
  \coordinate (leftB) at ({-2.5},{-2.5*tan(\beta)});
  \coordinate (rightB) at ({\rd},{\rd*tan(\beta)});

  \coordinate (bottomLeft) at (-\rr,-\rr);
  \coordinate (bottomRight) at (\rr,-\rr);
  \coordinate (topRight) at (\rr,\rr);
  \coordinate (topLeft) at (-\rr,\rr);


  % for debugging
  %\node[draw] at (0,0) {\filling};

  % fill
  \begin{scope}
    \path[clip] (O) ++ ({\r*cos(\beta)},{\r*sin(\beta)}) arc({\beta}:{\alpha}:\r)
                (O) -- ({\r*cos(\alpha)},{\r*sin(\alpha)}) -- ({\r*cos(\beta)},{\r*sin(\beta)})
                (O) -- ({-\r*cos(\alpha)},{-\r*sin(\alpha)}) -- (-{\r*cos(\beta)},-{\r*sin(\beta)})
                (O) ++ (-{\r*cos(\alpha)},-{\r*sin(\alpha)}) arc({180+\alpha}:{180+\beta}:\r)
                ;
    \fill[pattern=north west lines, pattern color=red]
        (bottomLeft) -- (bottomRight) -- (topRight) -- (topLeft) -- cycle;
  \end{scope}

  %%% circle
  \draw[thick,black!70!black]
    (O) circle (\r);

  %%% arc
  \draw[red,<->]
    (O) ++ (({\rd*cos(\beta)},{\rd*sin(\beta)}) arc({\beta}:{\alpha}:\rd);

  \draw[->,thick] (left) -- (right) node[right] {${\arctg{\frac{1}{2}}}$};
  \draw[->,thick] (leftB) -- (rightB) node[right] {$\arctg{2}$};
  \draw[] (\r,-\rd) -- (\r,8);
  \draw[] (\r,0) -- (rightAux) node [midway,right] {$\frac{1}{2}$};
  \draw[thick] (rightAux) -- (\r,7) node[right] {$2$};
  

\end{scope}
\end{tikzpicture}
\caption{Y!}
\end{figure}